import {Coordinate} from '../components/App/App.function';
import {TrackPointWorkerInput} from './workerPool';

export function calculateTrackPoint(input: TrackPointWorkerInput): Coordinate {
    return [0, 0];
}
